version: '3.7'

services:
  main:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ${컨테이너 명}
    volumes:
      - /home/ec2-user/silver/:/home/silver
    ports:
      - "80:80"
    privileged: true
